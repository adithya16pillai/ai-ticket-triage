# Where the SPA sends API calls. Default uses the Vite dev proxy (/api -> :8000).
# Override to hit a deployed API directly, e.g. https://api.example.com
VITE_API_BASE=/api

# Used only by the dev proxy in vite.config.ts
VITE_API_TARGET=http://localhost:8000
